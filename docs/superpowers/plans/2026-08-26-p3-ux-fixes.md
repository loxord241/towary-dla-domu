# P3 UX Fixes Implementation Plan (2026-08-26)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox syntax.

**Goal:** Implement all six current P3 items (R1 prices, N1 recent container, R3 category cards, R2 pagination semantics, N2 grid breakpoints) in one stage; R4 verified-but-unchanged (no browser).

**Architecture:** `formatPrice` becomes the single presentation formatter (uk-UA Intl grouping/comma decimals); pure display changes only — no data, cart, order, SEO or review logic touched.

**Tech Stack:** Next.js 16.3.1, TypeScript, node:test.

**Spec:** re-audit report 2026-08-26 (sections A/C) + this GO brief.

## Global Constraints

- NO git commits. DB 0 writes. No migrations/RLS/checkout/orders/auth/admin/importer/reviews/SEO/drawer changes.
- Discount math (`1 - price/old_price`), cart totals, page clamping stay byte-identical.
- Existing suites must stay green: discount-ui regexes pin ProductCard classes; ux-fixes pins cart formatPrice usage & search/badges; h1-invariants; pagination-hardening.
- Playwright UNAVAILABLE (browser downloaded but system deps missing, no sudo) → R4 unchanged, honest note in report.

---

### Task 1: formatPrice uk-UA formatting + all surfaces (P3-R1)

- Modify: `app/lib/format.ts`, `app/components/ProductCard.tsx`, `app/product/[slug]/page.tsx` (main price, old price line, variants ×₴→formatPrice(product.currency))
- Test: update `tests/format-price.test.ts`; add surface pins to new `tests/p3-ux.test.ts`

- [ ] Step 1: Update failing tests in tests/format-price.test.ts (group separator = NBSP `\u00A0`, decimal comma): 7399→'7 399 UAH', 99.5→'99,50 UAH', 0→'0 UAH'; add 17599 and currency-null cases
- [ ] Step 2: red run
- [ ] Step 3: implement with `new Intl.NumberFormat('uk-UA',{minimumFractionDigits:0,maximumFractionDigits:2})` module-level formatter
- [ ] Step 4: green run
- [ ] Step 5: swap raw renders in ProductCard (price+old_price), product page (main price, old_price, variants use product.currency); RelatedProducts/RecentProducts inherit via ProductCard

### Task 2: RecentlyViewed container (P3-N1)

- Modify: `app/components/RecentProducts.tsx` — drop outer `container mx-auto px-4 pb-4` section wrapper; keep inner card div as section root
- [ ] test pin (p3-ux.test.ts): file must NOT contain `container mx-auto` anymore; still returns null when empty

### Task 3: Category cards (P3-R3)

- Modify: `app/(home)/page.tsx` — «Переглянути товари →» always visible (remove opacity-0 group-hover transition); 5th card spans full row on mobile (`idx === 4 && 'col-span-2 sm:col-span-1'`)
- [ ] test pin: no `opacity-0 group-hover` in home; presence of col-span handling

### Task 4: Pagination disabled semantics (P3-R2)

- Modify: `app/catalog/page.tsx` — shared control class; disabled side renders `<span aria-disabled="true">` with unified geometry, muted text; URL/clamp logic untouched
- [ ] test pin: both disabled sides carry aria-disabled="true"; enabled Links unchanged

### Task 5: Grid breakpoints (P3-N2)

- Modify: `RelatedProducts.tsx`, `RecentProducts.tsx`, `(home)/page.tsx` popular grid → `grid-cols-2 … md:grid-cols-3 lg:grid-cols-4`
- [ ] test pin: each file matches md:grid-cols-3 and lg:grid-cols-4

### Task 6: Gates + live SSR verification + PROJECT_CONTEXT note + report

- npm test / tsc / lint / build; next start curl: prices on catalog/product («17 599 UAH», no ₴), single-container recent (client-rendered → verify via source), aria-disabled present; Playwright honest-unavailable note.

## Self-review

R4 explicitly out of code scope (no browser). All edits display-only; discount-ui regex targets class names that remain (text-red-600/line-through/text-gray-4x0/hasDiscount &&). format-price test rewritten for the NEW intended output (documented in file header).
